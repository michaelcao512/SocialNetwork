import { Button } from "@mui/material";

function EditUserInfo(props) {
    const { user, userInfo, onUserInfoUpdate } = props;
    return ( 
        <>
            <Button variant="contained" color="primary">Edit User Info (TODO)</Button>
        </>
     );
}

export default EditUserInfo;